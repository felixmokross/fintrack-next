name: updater

on:
  schedule:
    - cron: "0 7 * * *"

  workflow_dispatch:

jobs:
  updater:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Updates
        uses: ./.github/actions/call-updater
