export enum AccountCategoryType {
  ASSET = "ASSET",
  LIABILITY = "LIABILITY",
}

export enum AccountType {
  TRACKED = "TRACKED",
  VALUATED = "VALUATED",
}

export enum AccountUnitKind {
  CURRENCY = "CURRENCY",
  STOCK = "STOCK",
}
